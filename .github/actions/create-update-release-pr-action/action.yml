name: 'Create or Update Release branch + PR'
branding:
  icon: shopping-bag
  colour: green
inputs:
  GITHUB_TOKEN:
    description: 'GitHub access token'
    required: true
runs:
  using: 'node20'
  main: 'index.js'